[project]
name = "hinge-modelling"
version = "0.1.0"
description = "Precise 3D hinge mechanism models using build123d, exported as GLB for Three.js"
requires-python = ">=3.10"
dependencies = [
    "build123d>=0.7",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0",
]
